/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['healbitesv1.oss-ap-southeast-1.aliyuncs.com'],
    // Add other domains here if needed
  },
  // Your other Next.js config options
}

module.exports = nextConfig